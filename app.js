const inquirer = require('inquirer')

// Prompt user for 'New Game' or 'Leaderboard'

// display Categories when New Game is picked

// Prompt a series of 10 questions 

// If they are correct, tell them they were successful

// If they are incorrect, display a message that says they were incorrect and display the right answer

// Once all questions are done, display their total score

// Prompt user for a username to record their high score